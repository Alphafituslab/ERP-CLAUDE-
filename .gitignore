venv/
__pycache__/
*.pyc

data/*.db
data/*.db-journal
data/.jwt_secret

.dev_secret.txt
config_ambiente.bat
server_log*.txt
demo_log*.txt

*.log

# Saída de build do instalador (gerada por installer/alphafitus.spec +
# installer/AlphafitusOS.iss) — grande e 100% reproduzível a partir do
# código-fonte, nunca precisa ir pro controle de versão.
installer/dist/
installer/build/
installer/saida/
backups_manuais/
# Fase 138 — chave PRIVADA do túnel SSH reverso do Portal do Cliente
# (Terceirização Premium). NUNCA pode ir pro git nem ser embutida no
# instalador geral (qualquer um que baixasse o instalador ganharia acesso
# de encaminhamento de porta pro VPS) — provisionada manualmente, direto
# na máquina real, o mesmo tratamento dado a toda outra chave/segredo
# deste projeto (chave do banco, apikey da Evolution API, etc.).
tunel_portal/
